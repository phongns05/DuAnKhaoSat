@model IEnumerable<SurveySystem.Models.Difficulty>

@{
    ViewData["Title"] = "Quản lý Độ khó";
}

<div class="page-header">
    <div class="header-content">
        <h1><i class="fas fa-layer-group"></i> Quản lý Độ khó</h1>
        <p>Quản lý các mức độ khó của câu hỏi</p>
    </div>
    <div class="header-actions">
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus"></i> Tạo mới
        </a>
    </div>
</div>

<div class="data-table">
    <table class="table table-hover">
        <thead class="table-header">
            <tr>
                <th>
                    <i class="fas fa-layer-group"></i> Tên độ khó
                </th>
                <th class="text-center">
                    <i class="fas fa-cogs"></i> Thao tác
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="table-row">
                    <td>
                        <div class="item-info">
                            <span class="item-name">@Html.DisplayFor(modelItem => item.LevelName)</span>
                        </div>
                    </td>
                    <td class="action-buttons">
                        <a asp-action="Edit" asp-route-id="@item.DifficultyId" class="btn btn-sm btn-outline-primary" title="Chỉnh sửa">
                            <i class="fas fa-edit"></i>
                        </a>
                        <a asp-action="Details" asp-route-id="@item.DifficultyId" class="btn btn-sm btn-outline-info" title="Chi tiết">
                            <i class="fas fa-eye"></i>
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.DifficultyId" class="btn btn-sm btn-outline-danger" title="Xóa">
                            <i class="fas fa-trash"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@if (!Model.Any())
{
    <div class="empty-state">
        <i class="fas fa-layer-group"></i>
        <h3>Chưa có độ khó nào</h3>
        <p>Bắt đầu bằng cách tạo độ khó đầu tiên</p>
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus"></i> Tạo độ khó
        </a>
    </div>
}

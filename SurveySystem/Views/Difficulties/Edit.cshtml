@model SurveySystem.Models.Difficulty

@{
    ViewData["Title"] = "Chỉnh sửa Độ khó";
}

<div class="page-header">
    <div class="header-content">
        <h1><i class="fas fa-edit"></i> Chỉnh sửa Độ khó</h1>
        <p>Cập nhật thông tin độ khó</p>
    </div>
    <div class="header-actions">
        <a asp-action="Index" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left"></i> Quay lại
        </a>
    </div>
</div>

<div class="form-container">
    <form asp-action="Edit" class="needs-validation" novalidate>
        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
        <input type="hidden" asp-for="DifficultyId" />
        
        <div class="form-group">
            <label asp-for="LevelName" class="form-label">
                <i class="fas fa-layer-group"></i> Tên độ khó <span class="text-danger">*</span>
            </label>
            <input asp-for="LevelName" class="form-control" required />
            <span asp-validation-for="LevelName" class="text-danger"></span>
            <div class="form-text">Nhập tên độ khó (ví dụ: Junior, Middle, Senior)</div>
        </div>
        
        <div class="form-actions">
            <button type="submit" class="btn btn-primary">
                <i class="fas fa-save"></i> Lưu thay đổi
            </button>
            <a asp-action="Index" class="btn btn-outline-secondary">
                <i class="fas fa-times"></i> Hủy
            </a>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

@model IEnumerable<SurveySystem.Models.Skill>

@{
    ViewData["Title"] = "Quản lý Kỹ năng";
}

<div class="page-header">
    <div class="header-content">
        <h1><i class="fas fa-brain"></i> Quản lý Kỹ năng</h1>
        <p>Quản lý các kỹ năng trong hệ thống</p>
    </div>
    <div class="header-actions">
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus"></i> Tạo mới
        </a>
    </div>
</div>

<div class="data-table">
    <table class="table table-hover">
        <thead class="table-header">
            <tr>
                <th>
                    <i class="fas fa-brain"></i> Tên kỹ năng
                </th>
                <th class="text-center">
                    <i class="fas fa-cogs"></i> Thao tác
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="table-row">
                    <td>
                        <div class="item-info">
                            <span class="item-name">@Html.DisplayFor(modelItem => item.SkillName)</span>
                        </div>
                    </td>
                    <td class="action-buttons">
                        <a asp-action="Edit" asp-route-id="@item.SkillId" class="btn btn-sm btn-outline-primary" title="Chỉnh sửa">
                            <i class="fas fa-edit"></i>
                        </a>
                        <a asp-action="Details" asp-route-id="@item.SkillId" class="btn btn-sm btn-outline-info" title="Chi tiết">
                            <i class="fas fa-eye"></i>
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.SkillId" class="btn btn-sm btn-outline-danger" title="Xóa">
                            <i class="fas fa-trash"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@if (!Model.Any())
{
    <div class="empty-state">
        <i class="fas fa-brain"></i>
        <h3>Chưa có kỹ năng nào</h3>
        <p>Bắt đầu bằng cách tạo kỹ năng đầu tiên</p>
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus"></i> Tạo kỹ năng
        </a>
    </div>
}

@model SurveySystem.Models.Skill

@{
    ViewData["Title"] = "Chỉnh sửa Kỹ năng";
}

<div class="page-header">
    <div class="header-content">
        <h1><i class="fas fa-edit"></i> Chỉnh sửa Kỹ năng</h1>
        <p>Cập nhật thông tin kỹ năng</p>
    </div>
    <div class="header-actions">
        <a asp-action="Index" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left"></i> Quay lại
        </a>
    </div>
</div>

<div class="form-container">
    <form asp-action="Edit" class="needs-validation" novalidate>
        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
        <input type="hidden" asp-for="SkillId" />
        
        <div class="form-group">
            <label asp-for="SkillName" class="form-label">
                <i class="fas fa-brain"></i> Tên kỹ năng <span class="text-danger">*</span>
            </label>
            <input asp-for="SkillName" class="form-control" required />
            <span asp-validation-for="SkillName" class="text-danger"></span>
            <div class="form-text">Nhập tên kỹ năng (ví dụ: C#, JavaScript, Testing)</div>
        </div>
        
        <div class="form-actions">
            <button type="submit" class="btn btn-primary">
                <i class="fas fa-save"></i> Lưu thay đổi
            </button>
            <a asp-action="Index" class="btn btn-outline-secondary">
                <i class="fas fa-times"></i> Hủy
            </a>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

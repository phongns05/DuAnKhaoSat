@model IEnumerable<SurveySystem.Models.Department>

@{
    ViewData["Title"] = "Quản lý Phòng ban";
}

<div class="page-header">
    <div class="header-content">
        <h1><i class="fas fa-building"></i> Quản lý Phòng ban</h1>
        <p>Quản lý các phòng ban trong tổ chức</p>
    </div>
    <div class="header-actions">
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus"></i> Tạo mới
        </a>
    </div>
</div>

<div class="data-table">
    <table class="table table-hover">
        <thead class="table-header">
            <tr>
                <th>
                    <i class="fas fa-building"></i> Tên phòng ban
                </th>
                <th>
                    <i class="fas fa-info-circle"></i> Mô tả
                </th>
                <th class="text-center">
                    <i class="fas fa-cogs"></i> Thao tác
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="table-row">
                    <td>
                        <div class="item-info">
                            <span class="item-name">@Html.DisplayFor(modelItem => item.DeptName)</span>
                        </div>
                    </td>
                    <td>
                        <div class="item-description">
                            @Html.DisplayFor(modelItem => item.Description)
                        </div>
                    </td>
                    <td class="action-buttons">
                        <a asp-action="Edit" asp-route-id="@item.DeptId" class="btn btn-sm btn-outline-primary" title="Chỉnh sửa">
                            <i class="fas fa-edit"></i>
                        </a>
                        <a asp-action="Details" asp-route-id="@item.DeptId" class="btn btn-sm btn-outline-info" title="Chi tiết">
                            <i class="fas fa-eye"></i>
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.DeptId" class="btn btn-sm btn-outline-danger" title="Xóa">
                            <i class="fas fa-trash"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@if (!Model.Any())
{
    <div class="empty-state">
        <i class="fas fa-building"></i>
        <h3>Chưa có phòng ban nào</h3>
        <p>Bắt đầu bằng cách tạo phòng ban đầu tiên</p>
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus"></i> Tạo phòng ban
        </a>
    </div>
}
